# 开发专用说明

## 分支提交规则（必读）

- 日常开发、提交和推送只能在 `huangtong` 分支进行。
- 禁止直接修改、提交或推送 `main` 分支。
- 需要把 `huangtong` 的内容并入 `main` 时，只能由仓库维护者在 GitHub 上通过 Pull Request 审核、合并。
- 每次提交前必须确认当前分支为 `huangtong`，推送目标必须明确为 `origin/huangtong`。

---

## 0. 与 MCCA 的关系（必读）

**当前主线任务依赖 / 参考了 [MCCA](https://github.com/MaaXYZ/MCCA)。非常感谢该项目。**

| 本仓库路径 | 说明 |
|------------|------|
| `assets/interface.json` | 任务列表、option、`pipeline_override` 结构来自 MCCA 并已本地化改造 |
| `assets/resource/pipeline/base/` | 启动、礼包、贸易、军演、基建、每日探索、周本、领奖等主线 pipeline |
| `assets/resource/bilibili/` | B 服包名覆盖（StartApp/StopApp） |

自研内容（如 `pipeline/周常/`、`周本任务.json`、`开始游戏.json`）可并存，但 **全目录节点名全局唯一**，勿与 `base/` 重名。

改造约定：节点已尽量汉化；`interrupt` / `is_sub` 需按 MaaFW ≥5.1 使用 `[JumpBack]`。问题请在本仓库提 Issue，不要默认 upstream 到 MCCA。

---

## 1. 准备工作

1. 按项目教程配置好 OCR（解压到 `assets/resource/model/ocr/`，需有 `det.onnx` / `rec.onnx` / `keys.txt`）
2. 截图工具可用 [MFAToolsPlus](https://github.com/SweetSmellFox/MFAToolsPlus)（下载 Release，不要只克隆源码）
3. 安装调试器：

```powershell
python -m pip install MaaDebugger
```

## 2. 开始调试

先进入**本仓库根目录**（有 `assets`、`README.md` 的那一层），再启动：

```powershell
# 先 cd 到你自己电脑上的项目路径，例如：
# cd D:\code\MAA_Practice

cd <你的项目根目录>
python -m MaaDebugger
```

浏览器打开：[http://localhost:8011](http://localhost:8011)

### 连接 MuMu（本机路径因人而异）


| 字段     | 示例（按自己安装位置改）                |
| ------ | --------------------------- |
| 控制器    | Adb                         |
| ADB 路径 | `你的MuMu目录\nx_main\adb.exe`  |
| 地址     | `127.0.0.1:16384`（以多开器显示为准） |
| Config | `{}`                        |
| 资源目录   | 项目下的 `assets\resource`      |


不要直接复制别人的绝对路径（如 `C:\Users\xxx\...`），每人电脑用户名和盘符都不同。

更细的本地手册见：`docs/zh_cn/develop/local_dev.md`  
踩坑汇总见：`开发踩坑.md`

---



## 2.1 启动 Pipeline 可视化编辑器（MaaPipelineEditor）

用来**看 / 改任务流程图**，和 MaaDebugger（实机跑）不是同一个东西。

### 方式 A：只看图（最快，不用装本地服务）

1. 浏览器打开：[https://mpe.codax.site/stable/](https://mpe.codax.site/stable/)
2. 用编辑器打开某个 pipeline JSON（如 `assets/resource/pipeline/开始游戏.json`），全选复制
3. 在网页里点 **导入（粘贴板）**



### 方式 B：连本地项目（可保存回文件）

官网短链 `mpe.codax.site/install.ps1` 可能 404，用 GitHub 脚本：

```powershell
# 安装 LocalBridge（只需一次）
irm https://raw.githubusercontent.com/kqcoxn/MaaPipelineEditor/main/tools/install.ps1 | iex
```

```powershell
# 每次在「资源目录」启动（有 pipeline、image 的那层）
cd <你的项目根目录>\assets\resource
mpelb
```

再打开 [https://mpe.codax.site/stable/](https://mpe.codax.site/stable/) ，左下角连 `localhost:9066`。  
连上后才能用「保存到文件 / 用本地服务创建」。

### 注意

- 所有 `pipeline/**/*.json` 会合并，**节点名全局不能重复**
- 覆盖式导入可能冲掉当前图，重要改动前先 git commit
- 实机跑任务仍用第 2 节的 **MaaDebugger**（`http://localhost:8011`）

---



## 2.2 启动 Agent（自定义识别 / 动作）

跑带 `Custom` 的节点（如 `number_lt`）需要 Agent。  
`interface.json` 里的 `agent` 配置只对 **MaaPiCli / 正式客户端** 自动拉起；**MaaDebugger / MPE 调试不会自动启动**，要手动开。

**顺序很重要：先起 Pipeline 调试端，再起 Agent 服务。**  
（AgentServer 是去连调试端创建的标识符；反了容易 `agent client connect failed`。）

### 依赖

```powershell
python -m pip install maafw
```

（若已能跑 MaaDebugger，一般已装好。）

### 用 MaaDebugger 调试时（日常开发）

1. **先**按第 2 节启动 Debugger，连上模拟器，并成功加载 `assets\resource`
2. Debugger 页面 **Agent Identifier** 填一串，例如：`my-agent-1`（先填好，先别急着 Connect 也行）
3. **再**另开终端，在**项目根目录**启动 Agent（参数与 Identifier 一致）：

```powershell
cd <你的项目根目录>
python .\agent\main.py my-agent-1
```

1. 回到 Debugger，确认 Identifier 仍是同一串，点 **Connect**
2. 再跑含 `Custom` 的任务 / 节点



### 用 MPE（mpelb）流程级调试时

1. **先** `mpelb` + 网页连上 LocalBridge，资源加载成功，调试工作台里配好 Agent 标识符
2. **再** `python .\agent\main.py <同一标识符>`
3. 在调试工作台点「测试连接」或直接跑任务

注意：

- 两边 Identifier **必须完全一致**
- 必须带参数：只跑 `python .\agent\main.py` 会打印 Usage 并退出
- Agent 窗口保持开着；改完 `number_lt` 等代码需**重启** Agent 再 Connect
- 连不上时：确认资源已加载 → 先停掉旧 Agent → 再按「Pipeline → Agent → Connect」重来一遍



### 用正式客户端 / MaaPiCli 时

`assets/interface.json` 已配置：

```json
"agent": {
    "child_exec": "python",
    "child_args": ["./agent/main.py"]
}
```

客户端跑任务时会自动带 socket_id 拉起 `agent/main.py`，一般不用手动开。  
本机若 `python` 不在 PATH，把 `child_exec` 改成完整路径（如 `C:\\Python312\\python.exe`）。

---



## 3. 当前进度（截至 2026-08-30）

### 主线（依赖 MCCA，见第 0 节）

| 模块 | 文件 / 入口 | 说明 |
|------|-------------|------|
| 任务列表 | `assets/interface.json` | 启动、礼包、贸易、军演、基建、每日探索、周本、领奖、关闭、历战等 |
| 主线 pipeline | `pipeline/base/*.json` | 自 MCCA 迁移；节点名已汉化；扫荡/出击为通用模块 |
| B 服 | `resource/bilibili/pipeline/` | 覆盖 StartApp/StopApp 包名 |
| 通用子任务 | `base/通用.json` 等 | 回首页、关弹窗、战斗 UI 等 |

### 自研 / 并存

| 模块 | 文件 | 说明 |
|------|------|------|
| 旧启动链 | `pipeline/开始游戏.json` | 点游戏名进服（可与 MCCA「进入首页」并存，注意勿重名） |
| 旧周本草稿 | `pipeline/周本任务.json` | 宙斯点击链草稿 |
| 周常 | `pipeline/周常/` | JJC、基地、日常刷体力等 |
| Agent | `agent/number_lt.py` | Custom：ROI 数字 \< threshold |
| 调试 | MaaDebugger / MPE+mpelb | 资源目录：`assets/resource` |

### Pipeline 目录结构（简）

```
assets/resource/pipeline/
├── base/                 # ← MCCA 主线（请优先改这里）
│   ├── 启动游戏.json
│   ├── 通用.json / 通用-扫荡.json / 通用-出击选择.json
│   ├── 周本.json / 基建.json / 每日探索.json / …
│   └── …
├── 周常/                 # 自研
├── 开始游戏.json / 周本任务.json / 一键日常.json / …
└── …
assets/resource/bilibili/pipeline/   # B 服包名
```

### interface 已挂任务（MCCA 结构）

- `启动游戏` → `进入首页`
- `每日免费礼包` → `每日免费礼包`
- `限时贸易所购买` → `限时贸易所购买`
- `模拟军演` → `模拟军演`（override：`首页_出击`）
- `基建` → `基建`
- `每日探索` → `出击任务列表`
- `周本` → `周本`（override：`首页_出击` → `活动探索`）
- `领取奖励(邮箱+每日+通行证)` → `领取奖励`
- 以及活动 / 历战试炼 / 关闭游戏 等

> 注意：`首页_出击` → 后续去向常靠 **interface 的 `pipeline_override`**，MPE 画布上可能看不到连线。

---

## 4. 还缺什么 / 待修

### 高优先级

1. **MCCA 迁移后的实机验收**（启动、扫荡停机、周本、基建）
2. **自研周常与 base 整合 / 去重**（避免双套逻辑、节点重名）
3. **打包目录同步**：更新 `interface`/`resource` 后，MFW 本地 `config` 可能仍显示旧任务名，需刷新配置或重载资源

### 中优先级

1. 项目展示信息（名称、github、欢迎语）与发版说明
2. 异常弹窗 / 网络重连兜底
3. Agent 在 Debugger / MPE 下的连接体验（见 2.2）

### 低优先级

1. 进一步对齐上游 MCCA 更新（有选择地 cherry-pick，勿整仓覆盖丢自研）
2. 同学环境：MuMu ADB、OCR、MFAToolsPlus Release

---

## 5. 下一步建议（简短）

1. 主线以 `pipeline/base` + `interface.json` 为准做实机回归。
2. 改任务前先看是否已有 MCCA 同名能力，避免重复造轮子。
3. 能用界面特征判断的优先纯 JSON；必须比数字再用 Agent。
4. 改 pipeline 前先 commit；同步到桌面安装包时记得覆盖 `interface.json` + `resource`（及必要时刷新 `config`）。
