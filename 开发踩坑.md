# 开发踩坑记录

截至 2026-08-05，基地任务 / Agent / 模板匹配相关问题汇总。

---

## 1. Pipeline 解析失败：`key is empty`

**现象**

```
PipelineResMgr::parse_and_override_once key is empty [key=]
open_and_parse_file failed [.../周常/基地任务.json]
```

**原因**

JSON 里有一个键名为空字符串 `""` 的节点（多半是 MPE 编辑器残留，只有 `$__mpe_code` 坐标，没有识别/动作）。MAA 不允许空 key。

**处理**

删掉该空节点后重新加载资源。

---

## 2. Agent 连不上：`agent client connect failed`

**现象**

```
agent client connect 失败: agentId=my-agent-1 ... timeoutMs=2000
Failed to connect AgentClient.
```

**原因**

- Debugger / MPE **不会**按 `interface.json` 自动拉起 Agent，要手动开 `agent/main.py`
- 连接方向：调试端是 **AgentClient**（先建监听），`main.py` 是 **AgentServer**（去连标识符）
- **先起 Agent、再开 Pipeline** 容易连不上

**正确顺序**

1. 先起 Debugger 或 MPE（mpelb）→ 资源加载成功 → Identifier 填好  
2. 再起：`python .\agent\main.py my-agent-1`  
3. 再点 Connect / 测试连接  

两边 Identifier 必须完全一致。详细步骤见 `开发专用.md` §2.2。

**补充**

`Toolkit.init_option is deprecated` 只是警告，不影响启动。

---

## 3. 灰色「确定」误点亮色按钮

**节点**：`制作不了`（模板 `灰色制造确定.png`）

**原因**

默认 `method: 5`（CCOEFF）偏形状、颜色不敏感；灰态/亮态「确定」字形相近，容易都过默认 `threshold: 0.7`。

**处理**

```json
"method": 10001,
"threshold": 0.92
```

`10001` 更吃颜色；仍误点就再提高阈值，或重截更灰、特征更明显的模板。

---

## 4. 「找到支援卷」识别太松

**节点**：`找到支援卷`

**原因**

1. 模板截太大（整块面板：图标 + 材料 + 黄底「交付」），公共 UI 多，别的订单也像  
2. 默认阈值/算法不够严

**处理**

- **优先重裁**：只留订单图标等独特区域，去掉大黄「交付」条  
- 参数收紧，例如：

```json
"method": 10001,
"threshold": 0.9
```

（后续可再调到 0.93~0.97，或加 `roi`）

---

## 5. 「刷新好友」左右箭头分不清

**节点**：`刷新好友`（模板 `点击下一位好友.png`）

**原因**

1. 左右箭头几乎镜像，小三角模板又糊，两边都容易过阈值  
2. 默认 `order_by: Horizontal` + `index: 0` → 多个命中时点**最左边**（上一位）

**处理**

```json
"method": 10001,
"threshold": 0.9,
"order_by": "Score",
"index": 0
```

仍不稳时：

- 给右箭头加 `roi`（只搜右侧）最稳  
- 或 `order_by: "Horizontal"` + `index: -1`（点最右边）  
- 模板带一点右侧按钮周围特征，别只裁三角

---

## 6. `method` / `threshold` 文档在哪

本仓库 `开发专用.md` **没有**写这两个字段。

| 来源 | 路径 |
|------|------|
| 官方文档 | https://maafw.com/docs/3.1-PipelineProtocol/ （TemplateMatch 一节） |
| 本地 schema | `deps/tools/pipeline.schema.json` → `TemplateMatch` |

| 字段 | 含义 | 默认 |
|------|------|------|
| `threshold` | 匹配分数门槛，越高越严 | `0.7` |
| `method` | `5` 颜色不敏感（常用）；`10001` 颜色敏感、适合精确区分 | `5` |

---

## 模板匹配经验小结

1. **模板要小、要独特**，少带公共按钮/版式  
2. 要区分颜色/灰亮态 → 优先 `method: 10001` + 提高 `threshold`  
3. 全屏多个相似目标 → 用 `roi`，或改 `order_by` / `index`  
4. 左右对称 UI → 特别小心默认「从左往右取第 0 个」
